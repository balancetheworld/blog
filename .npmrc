# pnpm 10 配置
# 允许运行构建脚本（better-sqlite3 等原生模块需要）
enable-pre-post-scripts=true

# 使用 shamefully-hoist 避免 flat 结构问题
shamefully-hoist=true

# 明确允许这些包的构建脚本运行
# pnpm 10 需要这个配置来启用原生模块编译
ignore-scripts=false
